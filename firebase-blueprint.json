{
  "entities": {
    "PortalData": {
      "title": "PortalData",
      "description": "Shared operational portal states, announcement, licensing and Google connections",
      "type": "object",
      "properties": {
        "issuedLicenses": {
          "type": "array",
          "description": "Completed flight training licenses"
        },
        "inventory": {
          "type": "array",
          "description": "Active aircraft flight school components"
        },
        "aircraftList": {
          "type": "array",
          "description": "The current pilot aircraft training catalog"
        },
        "announcement": {
          "type": "string",
          "description": "The global live notification banner text"
        },
        "googleConnectionType": {
          "type": "string",
          "enum": ["webapp", "auth"],
          "description": "The active spreadsheet transmission pathway"
        },
        "sheetsWebAppUrl": {
          "type": "string",
          "description": "Proxy macro URL for sandboxed Apps Script writes"
        },
        "savedSpreadsheetId": {
          "type": "string",
          "description": "The target Sheets identifier"
        },
        "financialConfig": {
          "type": "object",
          "description": "Adjustable financial rates, including brevet pricing, wages, taxes, and cost matrices"
        },
        "bonuses": {
          "type": "array",
          "description": "The bonuses issued per person"
        },
        "activeSessionId": {
          "type": "string",
          "description": "The active unique token for the logged in staff portal user session"
        },
        "activeSessionUser": {
          "type": "string",
          "description": "The name of the user who holds the active session"
        }
      }
    }
  },
  "firestore": {
    "/portal/data": {
      "schema": "PortalData",
      "description": "The single document housing the global state configuration for the school database"
    }
  }
}
